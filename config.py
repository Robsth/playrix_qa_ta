from pathlib import Path

PROJECT_ROOT = Path(__file__).resolve().parent
UI_ELEMENTS_ROOT = PROJECT_ROOT / 'ui_elements'
SCREENSHOTS_ROOT = PROJECT_ROOT / 'screenshots'
ENVS_ROOT = PROJECT_ROOT / 'envs'
